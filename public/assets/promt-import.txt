1. Struktur Tabel

```php
Schema::create('subjects', function (Blueprint $table) {
            $table->id();
            $table->string('subject_code')->comment('MAT');
            $table->string('subject_name')->comment('Matematika');
            $table->timestamps();
        });
```

2. Kode Model

```php
protected $fillable = ['subject_code', 'subject_name'];
```

3. Untuk tabel ini gk ada relasinya.
4. untuk subject_code ini bisa gk isi, jadi jika kolom bagian subject_code ini kosong maka akan dibuatkan kode secara otomatis. Sebenarnya aku udh buat fungsi untuk generate kodenya, akan aku lampirkan, kalo gk butuh juga gk msalah:

```php
public static function generateCode(string $name): string
    {
        $words = explode(' ', $name);

        if (count($words) === 1) {
            return strtoupper(substr($name, 0, min(3, strlen($name))));
        }

        $code = '';
        // Jika lebih dari 1 kata
        $code = '';
        foreach ($words as $word) {
            if (in_array(strtolower($word), ['dan', 'atau', 'serta', 'dengan', '&'])) {
                continue;
            }
            $code .= strtoupper(substr($word, 0, 1));
        }

        return $code;
    }
```

5. Menurutmu lebih baik yg mana? aku ngikut aja, btw ini untuk data Mata Pelajaran.

Kira segitu informasi yg bisa aku berikan, apakah sudah cukup? jika kurang tinggal bilang aja

Ringkasan Pertanyaan per Tabel
Untuk setiap tabel yang mau dibuatkan import, cukup jawab:

Nama tabel dan kolom-kolomnya (tipe + nullable/tidak)
Kode Model-nya (bagian $fillable dan relasi)
Kolom mana yang foreign key, dan user akan input nilai apa di Excel untuk kolom itu?
Apakah ada kolom yang di-generate otomatis (tidak perlu diinput user), misalnya book_code, slug, dll.?
Apakah boleh update data yang sudah ada (upsert), atau hanya insert saja?
